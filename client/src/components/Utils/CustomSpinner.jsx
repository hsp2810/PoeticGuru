import { Spinner } from '@chakra-ui/react';
import React from 'react';

const CustomSpinner = () => {
  return (
    <Spinner size="xl" margin={'5rem auto'} speed="1s" display={'block'} />
  );
};

export default CustomSpinner;
